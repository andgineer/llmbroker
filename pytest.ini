[pytest]
addopts = --doctest-modules
asyncio_mode = auto
asyncio_default_fixture_loop_scope = session
asyncio_default_test_loop_scope = session
markers =
    docker: requires Docker (testcontainers); skipped on platforms without Docker
