"""Contracts: what a registry / secrets / store must do, and what the tool loop drives.
Implement these to add one of your own; the shipped implementations live in
``llmbroker.standalone`` and the per-driver packages."""
