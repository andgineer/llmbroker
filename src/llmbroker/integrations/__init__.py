"""Host-side hooks for coexisting with third-party tools (e.g. Alembic).

These modules carry no backend logic and import no drivers; they are thin
shims a host wires into another tool's configuration.
"""
